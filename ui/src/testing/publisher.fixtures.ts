import { Publisher } from '../app/publisher';

const publisher1: Publisher = {} as Publisher;
publisher1.publisher = 'DC Comics';
publisher1.seriesCount = 4;
publisher1.series = [];

export { publisher1 };
