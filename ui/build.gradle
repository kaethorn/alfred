plugins {
  id 'java'
  id 'com.github.node-gradle.node' version '2.2.3'
}

node {
  download = false
  npmInstallCommand = 'ci'
}

npmInstall {
  args = ['--loglevel', 'error']
}

jar.dependsOn 'npm_run_buildProd'

jar {
  from 'dist/alfred' into 'static'
}
