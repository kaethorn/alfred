FROM gradle:5.5-jdk11 as builder

# Install Chrome
RUN apt-get update && apt-get install -y --no-install-recommends \
	gnupg apt-transport-https \
	&& curl -sSL https://dl.google.com/linux/linux_signing_key.pub | apt-key add - \
	&& echo 'deb https://dl.google.com/linux/chrome/deb/ stable main' > /etc/apt/sources.list.d/google-chrome.list \
	&& apt-get update && apt-get install -y --no-install-recommends \
	google-chrome-stable fontconfig fonts-ipafont-gothic fonts-freefont-ttf \
	&& apt-get purge --auto-remove -y gnupg \
	&& rm -rf /var/lib/apt/lists/*

# Build, unit test & lint
WORKDIR /workspace/app
COPY settings.gradle build.gradle ./
COPY src src
COPY ui ui
COPY config config
RUN gradle -q build

# Start the application and run tests
RUN nohup bash -c 'gradle -q bootRun > /dev/null &'

# Wait for app to start
RUN while ! curl --output /dev/null --silent --head --fail http://localhost:8080; do sleep 1; done

# Start UI tests
CMD gradle -q npm_run_e2eHeadless

